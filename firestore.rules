rules_version = '2';
service cloud.firestore {
  	match /databases/{database}/documents {
    match /deudores/{document=**} {
      allow read, write: if true;
    }
    match /{document=**} {
      allow read, write: if
          request.time < timestamp.date(2030, 4, 10);
    }
  }
}